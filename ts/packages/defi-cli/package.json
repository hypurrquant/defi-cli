{
  "name": "defi-cli",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "bin": {
    "defi": "dist/main.js"
  },
  "files": [
    "dist",
    "config"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@hypurrquant/defi-core": "workspace:*",
    "@hypurrquant/defi-protocols": "workspace:*",
    "commander": "^13.0.0",
    "picocolors": "^1.1.0",
    "viem": "^2.23.0"
  }
}
